<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Project.Group.GroupMapper">

    <insert id="createGroup">
        INSERT INTO group (
                           group_name,
                           group_user_id
        ) VALUES (
                     #{group_name}
                     #{group_user_id}
                 )
        w
    </insert>

</mapper>